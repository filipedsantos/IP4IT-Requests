// #HEADER
header.header
    nav.nav.nav--title
        a.nav__el(href='/') IP4IT Requests
    //- .header__logo
    //-     img(src='/img/logo-white.png' alt='IP4IT')
    nav.nav.nav--user
        if user
            //- a.nav__el.nav__el--logout(href='#') Log out
            //- a.nav__el(href='')
            //-     span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log In
            a.nav__el.nav__el--cta(href='#') Sign Up